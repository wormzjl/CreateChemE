# Five-heavy-fraction regrouping implementation plan

Date:2026-09-17. **Planning only.** The present change organizes research and removes Git tracking; it does not yet replace material data, run a new convergence campaign, train weights, or certify the game.

Confirmed after the plan review: use the five estimated fractions with sensitivity checks; retire production CDU17; fit one global Dalia mixture correction; train from classical-only labels; build the extensibility framework last. **No backward compatibility is required at this development stage.** This supersedes the earlier held-network/remapping discussion. No old-save migration, retired-species transfer, automatic old-property adaptation, compatibility aliases or backup catalog are deliverables. Use fresh development worlds; obsolete state must not be silently interpreted as the new basis or automatically erased.

## 0. Capture the old-basis reference before replacing data

This is a prerequisite, not an optional comparison. Before retiring records or changing properties, capture the default, literature and six crude preset requests with their source commit, package fingerprints, initializer strategy and acceptance outcome. Record feed molar/mass/standard-volume throughput; every side draw's molar/mass/standard-volume rate, temperature and mean MW; overhead and bottoms flows/compositions; stage temperatures; duties; dew-point ratio; iterations and runtime. State the volume reference conditions. Store the compact numerical report under `research/crude-regrouping/baseline/`, not a duplicate production catalog or backup model.

Use accepted old-basis solutions to establish physical draw-throughput targets. An input alone cannot determine a draw's MW or mass flow. If an old request does not converge, retain that failure and leave unavailable derived quantities explicitly missing; do not invent target yields. Resolve the missing reference or document an independently sourced target before claiming that case's physical draw yield is preserved. Old profiles remain comparison evidence only and may not initialize or label new-basis training solves (option 6B).

## 1. Fixed scope and component contract

Replace six TJL heavy pseudocomponents with five:

| New stable ID | Lower°C | Upper°C |
|---|---:|---:|
| `crude_pc08` |377.8749814474507|450|
| `crude_pc09` |450|550|
| `crude_pc10` |550|650|
| `crude_pc11` |650|750|
| `crude_pc12` |750|open|

Keep the original exact PC07/H1 boundary651.0249814474507K. All cut temperatures are characterization NBPs, not column operating temperatures. IDs do not encode a false chemical identity; use the existing localized cut templates and estimation metadata. Do not reuse old PC08–PC13 IDs for changed materials or silently alias an old component into several new ones.

Rename the entire active crude pseudocomponent family consistently to `crude_pc01` through `crude_pc12`: old `tjl19_pc01`–`tjl19_pc07` retain their boiling ranges and physical/EOS data under the new IDs; the five recharacterized heavy cuts become PC08–PC12. H1–H5 below are research shorthand only, never a second production naming scheme. Exact chemical IDs remain unchanged. English and translated displays continue to use NBP-range labels, with IDs in tooltips; the final cut says "above 750°C". No legacy aliases are required. Update every reference, filename, translation descriptor, test fixture and model axis together.

Retain exact chemicals and the first seven pseudocomponents in their declared order, followed by the five heavy cuts. The main methane/crude packages become 19 hydrocarbons; the methane-free TJL package becomes 18; the pipeline nitrogen extension becomes 20 non-water components. Water retains its existing separate model/treatment.

Audit all nine current production packages. Rebuild these eight IDs: `createcheme:tjl19_dwsim`, `createcheme:tjl20_methane`, `createcheme:tjl20_methane_nitrogen`, `createcheme:wti_light_export_tjl20`, `createcheme:upper_zakum_tjl20`, `createcheme:bonga_tjl20`, `createcheme:dalia_tjl20`, and `createcheme:cold_lake_blend_tjl20`. Package/assay IDs can stay stable while revisions/fingerprints change; numeric package names are not component-count assertions. Consistent renaming in this decision concerns pseudocomponents, not gratuitous changes to exact chemicals or crude preset names.

Retire `createcheme:cdu17_tjl_acs2018` from the production catalog and preset UI. Remove its production-only assay, properties, components and interactions when no active record references them. Do not rebin it or invent C4 isomer composition. Retain its independent Kesler–Lee/literature numerical fixtures as test-only references; replace tests that demand production parity with this retired dataset by clearly scoped independent-reference tests.

Concrete test-retention mechanism: put the unchanged CDU17 scientific JSON and a test-only index in a dedicated `src/test/resources` catalog overlay, with shared exact-chemical dependencies explicitly resolved. Load it through test helpers using the same catalog parser and scope it to each test; never register it in the production index, UI or runtime fallback. Keep the existing independent Java/Kesler–Lee oracles and numerical pins. The review identifies about 40 dependent test classes, including PR/flash/Jacobian tests: route these to the test catalog rather than changing their physics while regrouping production. Replace the production PILOT/load-path default in the same step. These are executable test fixtures, not a supported legacy dataset or save-compatibility feature.

Deferred: elemental/SARA/reactivity/wax append schema, reaction solvers, crude-specific transport fitting, non-Newtonian pipe laws, NRTL solver, new VDU topology/features. Existing VDU-compatible solver paths are qualification targets, not a reason to claim new VDU functionality.

## 2. Replace datasets and characterization

1. Inventory material components, selected property datasets, PR parameters/interactions, assays, standalone material index, fluid calibration/conditional-solute transport, preset defaults, localization descriptors, serialized fixtures and neural consumers. Produce a coverage table with an explicit action for every record; exact chemicals and water are reviewed/preserved, not refitted merely because their packages change.
2. Rebin the five producer assays from the transcribed source TBP and disjoint source-cut masses/densities in`research/crude-assays/source-data.json`. Preserve light-end subtraction, rounding normalization and each crude's mean-constrained exponential tail. Integrate the tail rather than truncating it at an invented endpoint. Recompute mass/mole/standard-volume conversions using the new MW/density records.
3. **Tia Juana reconstruction is estimated.** The retained DWSIM file contains 13 already-lumped components, not an original TBP curve. Construct a constrained monotone cumulative distribution from their amounts and estimated cut boundaries; use PCHIP as the primary reconstruction and piecewise-linear interpolation as a sensitivity alternative. Derive consistent within-cut density/MW profiles from the representative source points and constrain integration to conserve original total mass and reference volume, with the first seven cut amounts unchanged on the selected physical feed basis. Document the terminal-tail form and constrain its mass/volume/moments; no arbitrary terminal endpoint. If those constraints cannot be met with positive, physically ordered properties, stop and revise the reconstruction rather than silently dropping one constraint. Report sensitivity of each heavy cut's amount/NBP/MW/SG and default column yields. The five producer assays retain their own measured/transcribed curves and estimated tails; do not replace them by this synthetic Tia curve.
4. Define the shared new thermodynamic cut properties from **Tia Juana**, consistent with the existing shared-surrogate approach, not from unannounced pooled crude weights. Derive representative NBP, SG/density and MW from the constrained reconstruction. A DWSIM characterization API may estimate downstream properties from these explicitly reconstructed inputs, but cannot recover a missing original assay. Verify method/units and derive Tc/Pc/omega/Cp/enthalpy consistently; do not arithmetic-average critical constants or take the midpoint of an open-ended cut. Check aggregate residue density/MW against available producer data and report surrogate mismatch. Preserve the methane-enriched assay's synthetic 0.5 mol% methane convention after recomputing the base. Exact old-boundary merging was considered and not selected because the user chose the round grid aligned with the producer 450/550°C data.
5. Preserve total feed mass and reference-volume accounting; report the change in pseudomoles caused by characterization explicitly. Reconcile Cp/enthalpy curves on mass basis as well as the solver's molar basis. Retain explicit PR zero-interaction policy and valid water cross-treatment; validate every regenerated interaction reference.
6. Replace all affected external JSON records, indexes, package revisions/fingerprints, appearances and localized cut descriptors. Remove unused old production records. No duplicated legacy catalog, backup model or compatibility copy is required. Existing research observations are evidence, not a fallback production dataset.
7. Update basis sizes/index offsets in presets, GUI, codecs, fluid model and neural readers. Old results/warm starts/weights must fail contract checks. This is a breaking development release: no old-save adapter, held-network migration workflow or retired-species conversion is required. Remove obsolete compatibility allowlists and aliases where applicable. Reject incompatible persisted state explicitly; do not reinterpret old positional arrays or silently reset inventories. Current-format save/reopen and multiplayer round trips remain required.
8. Re-derive molar feed constants from the intended physical throughput (including the literature 100,000 bbl/day), new MWs and reference densities. For gameplay defaults preserve intended physical draw yields and recalculate molar draws; retain original thesis molar draw values in the independent literature comparison. Re-measure duty, phase and yield differences and feasibility near draw-supply limits; do not simply re-pin the old numbers without explanation.

Reconstruction sensitivity must cover two independent choices: (a) cumulative amounts located at the estimated cut-edge knots, versus cumulative half-lump amounts located at representative NBP knots; and (b) PCHIP versus piecewise-linear interpolation. This creates four reconstruction cases under the same conservation and positive-property constraints. Report the spread in cut amounts, NBP/MW/density and accepted column yields; comparing interpolants on one assumed knot placement alone understates uncertainty. The knot alternatives are estimation conventions, not measured distributions.

No-compatibility behavior is deliberate: column inputs recognized as using a retired basis report `INCOMPATIBLE` rather than silently resetting with `CORRUPT_PERSISTED_STATE`; unsupported fluid checkpoints may refuse startup with a message naming the basis change and fresh-world/explicit-data-reset remedy. Do not erase the developer's `run/` data automatically. Remove both the obsolete `compatibleAmbientExtension` hash allowlist and the old `tjl20_methane` island-ID adapter. Regenerate active fluid checkpoint fixtures for the current basis. Derive simple production widths and special-species positions from the current package/IDs during the initial change wherever the code already has that context; the broader extensibility framework still stays last.

### Test migration and interim build gate

Classify every dataset-coupled test before regeneration: independent mathematical/literature oracle (retain and report scientific differences), production golden vector (regenerate independently), structural width/ID assertion (derive from the package and IDs), empirical iteration/performance pin (re-measure), or obsolete production case (remove with reason). The review found 91 of 197 classes mentioning the old packages and 38 files with fixed widths/indices; refresh that inventory at implementation. Replace old DWSIM-viscosity goldens where their subject is the newly fitted liquid family; retain independent unit/correlation tests with explicit scope. Update assay fixtures, restart/packet fixtures, wet-boundary/stalled-state vectors and neural digests deliberately. Do not make a test regenerate its own expected answer from the implementation under test.

Use one integration branch. During the data change, retire incompatible F0 weights and F0-only promotion pins and exercise the existing unavailable-model/classical fallback. Keep each implementation gate buildable and tested; do not leave the main branch in a half-migrated state. Broader future-compound infrastructure is scheduled last in section 7, but the new basis must already work consistently in all current consumers.

Gate: parser/reference validation passes; every active package loads; fractions are finite/nonnegative and close to unit total; exact light-end and source-cut mass balances close to existing tolerances; no gaps/overlaps; all affected records have the new revision; no obsolete production heavy IDs remain referenced.

## 3. One global viscosity family

Chosen donor: **Dalia**. The retained source-rebinned liquid curves were compared at40,50,60,80,100,120,150°C across13 pseudocomponents. Mean absolute distance from the pointwise median in natural-log viscosity was Dalia0.01266, Upper Zakum0.18895, Bonga0.22392, WTI1.55683, Cold Lake2.18774. These are comparative scores over partially extrapolated curves, not experimental fit errors. The selection is reproducible in`select_viscosity.py`.

- Build the12 TJL pseudocomponent liquid curves from the same Dalia source-cut Walther family on the new cut grid. Use source overlaps and the declared mass/log blending rule, not old whole-crude aggregate fits. Retained PC01–PC07 also receive this shared family where source data exist; the three unmeasured light source intervals retain explicit light-cut estimates.
- For H3/H4/H5, the available source supplies one unresolved550°C+ curve. Initially share that curve; do not invent a viscosity gradient above550°C. At100°C the current Dalia source-derived heavy curve is about2.525Pa·s. Distinct heavy thermodynamic properties can coexist with this transport approximation.
- Apply the identical curve for each matching pseudocomponent in every active crude package. Keep exact-chemical, water, nitrogen and valid vapor-phase transport separate; CDU17 is retired.
- **Approved option 4A:** extend the current log-mole blending rule with one shared composition/temperature-dependent interaction correction fitted jointly to Dalia whole-crude and ideal 370°C+ residue measurements after the new MWs are frozen. Use a symmetric pair/group formulation whose correction vanishes in every pure-component limit. Apply exactly the same coefficients to every crude and blend; never branch on crude identity. This is not a uniform viscosity multiplier. Do not reuse the earlier per-crude coefficients or pour-point/yield-stress terms. No appended chemical state is needed. Keep exact-gas/water handling intact and qualify the correction's behavior under dilution and near zero-support components.
- Register fit/held-out temperatures before fitting. Report both corrected and uncorrected donor results through the actual production lookup/blending route. Working acceptance targets: improve both whole-crude and residue held-out errors, held-out mean absolute relative error at most 10% and maximum at most 20%, preserve pure source-cut curves and positive finite smooth mixture behavior. If data/rheology limitations make these gates unattainable, report the limitation rather than tuning on held-out observations or silently weakening the gate. Recompute the review's cross-crude comparison table on the new basis; mismatches for other crudes remain explicitly accepted, not individually fitted away. The old 0.874%/3.87% scores and the donor-selection distance are not claimed accuracy for this global model.
- Publish positive finite SI dynamic-viscosity tables/correlations with explicit units, conversion density and extrapolation flags. Preserve the current liquid-table range **293.15–900 K**, checking both 293.15 and 298.15 K, and requalify the new Cp/enthalpy continuation over 293.15–298.15 K. Make 0–500°C comparison plots as research, marking unqualified liquid continuations. Table coverage is not permission to expand the thermodynamic operating envelope or claim a stable liquid at 500°C.
- Verify pure-component limits, continuity, monotonic liquid-temperature behavior in the selected range, blending, and the pipeline's actual lookup route. Add an independent unit check preventing the old extra-density fallback defect. Compare all six crude presets and ideal370°C+ residues before/after on linear/log plots; mismatches between different crude measurements are accepted and reported.

Gate: one shared family and one shared correction are consumed by all active crude packages; donor holdout gates pass; transport is finite/positive at admitted states; both 293.15 and 298.15 K work in generator validation; no crude-specific quality or gel state is introduced. The unresolved 550°C+ pure curves do not establish increasing viscosity with deeper cuts. Any mixture-correction dependence on their proportions is empirical, not measured differentiation of the heavy tail.

### Global-fit registration and accepted tradeoff

Use at most four fitted coefficients against the six available Dalia bulk observations: whole crude at 20/40/50°C and ideal 370°C+ residue at 50/60/100°C. Register four fit points (whole 20/50, residue 50/100) and two unused interpolation checks (whole 40, residue 60). Four coefficients for six observations leave only two residual degrees of freedom; the 10%/20% gate checks donor interpolation, not universal mixing physics. Pure-cut consistency, dilution/pure limits, numeric stability and pipeline qualification are separate gates. Do not use other crudes to tune these global coefficients.

The updated review's exploratory calculation on the OLD 13-cut grid, with OLD Dalia coefficients applied globally, reports these predicted/measured ranges. They are an illustration of transfer limitations, not a prediction or accepted numerical result for the new grid:

| Crude | Whole, uncorrected → corrected | Residue, uncorrected → corrected |
|---|---|---|
| Dalia |0.50–0.55 →1.00–1.01|0.72–0.82 →1.00–1.01|
| Bonga |0.60–0.61 →1.13–1.20|0.30–0.57 →0.41–0.71|
| Upper Zakum |0.65–0.74 →1.23–1.47|1.10–1.44 →1.34–2.01|
| WTI Light |0.73–0.74 →1.36–1.40|2.32–4.94 →2.88–6.80|
| Cold Lake Blend |0.04–0.11 →0.07–0.20|0.03–0.11 →0.04–0.14|

Acceptance policy follows the user's explicit global-approximation choice and option 4A: non-donor measurement mismatch, including a regression versus the uncorrected family, is reported and is not by itself a rejection or a reason to add per-crude fits. Recompute this full table after new properties are frozen. This choice does not waive conservation, units, finite/positive transport, solver stability or the declared domain. If new results introduce a qualitatively different limitation, report it explicitly.

Keep the hot-path evaluation O(c) using factored sums, with a small direct pair-sum test oracle. Store the shared coefficients and per-component transport correction descriptors in validated catalog data covered by the viscosity fingerprint; bump the mixture-rule revision. Register descriptor meanings before fitting: correction participation is 1 for the selected crude pseudocomponents and 0 for exact chemicals, water, nitrogen and conditional-solute gases; their h/E entries are zero and no supercritical pure-liquid slope is requested. For participating cuts, h is the documented fixed 370°C+ retention descriptor and E is the positive liquid-curve slope over 40–50°C after unit conversion. Zero participation excludes a component from pair terms while its amount still dilutes participating mole fractions. New compounds default to no correction participation until explicit transport qualification. Refit the global coefficients for this declared form; do not copy the exploratory coefficients. This is transport-model data, not the deferred elemental/SARA append task.

## 4. Classical column qualification comes first

Disable learned seeds and saved/warm results explicitly (`CURRENT_ONLY`) on the new fingerprint. Use the required stage-0 baseline; do not preserve a duplicate legacy production dataset. Old outputs are comparisons, not equality targets for the newly characterized physics.

Freeze a request matrix by inputs before solving: six crude presets, relevant alternate packages, dry/wet operation, supported condenser branches, stage counts2–64, feeds and duties across the admitted range, no/one/multiple pumparounds and draws, feed stage, light/heavy blends and trace support. Include current default/preset and known difficult cases. Use existing independent Holland, derivative, flash and manufactured-system tests as unchanged numerical oracles.

Report native strict/advisory/nonconverged/infeasible outcomes separately; request-only infeasibility may be excluded with a reason, solver failures remain in denominators. Measure iterations, elapsed time, residual and final-step certificates, material/energy/water closure, phase support and mass-based product yields. Check property derivatives and flash stability before adjusting initializer heuristics. Fix genuine solver defects without relaxing scientific tolerances or reclassifying failures as physical solidification.

Include existing supported low-pressure/VDU-like flash and column probes and report their actual pressure/topology limits. A50kPa package minimum or unsupported VDU condenser/reboiler arrangement is not qualified for a deep-vacuum tower by this work. Expansion beyond the existing envelope needs a separate explicit qualification, not a new label on an old preset.

Gate: all declared gameplay/default cases pass strict acceptance; independent science tests pass; difficult-matrix outcomes and any regressions are accounted for. Establish a reproducible contemporary classical control before training.

Freeze the characterized properties, interactions, component order, caloric references and label-generation contract at this gate. Physics edits invalidate affected labels/anchors. Transport-only edits invalidate pipeline qualification; they do not automatically invalidate a column label if the column equations do not use that transport data.

## 5. Fresh transformer generation, following the V4 guide

Follow`documentation/V4_TRANSFORMER_TRAINING_GUIDE.md`, adapting its F0-specific dimensions, hashes, populations and paths to the new scientific contract. Its old counts and success rates are historical references, not acceptance denominators for this basis.

**Reader/encoding work:** current`V3AnchorTransformerInitializer` hardcodes20 components,74 global inputs,96 node inputs and85 anchor/output coordinates. Under the same encoding with19 hydrocarbons these become73 global,95 node-before-branch,81 anchor/output, and180 joined node inputs (95+81+3+1). Audit every offset, tensor shape, bound, normalization and Python twin. Bump the feature revision and reject old weights. This is a from-scratch route, not the guide's byte-identical F0 continuation route.

**Coverage:** train over all six main crude compositions and blends on the shared 19-component scientific basis, including no-pumparound and 45–64-stage difficult cells. Initially qualify an explicit set of equivalent package contracts; never ignore fingerprints to make a preset eligible. Section 7 replaces this with a physics-only ordered-sub-basis fingerprint, excluding assay identity but including EOS, properties, relevant interactions, water treatment and admitted domain. Methane-free and nitrogen-extended requests remain classical unless explicitly inside a qualified model domain. CDU17 is retired. Pipeline operation does not require a column neural model. Register a composition-domain check covering declared training mixtures/blends; independent per-feature min/max bounds alone do not establish coverage of arbitrary mixtures.

**Data and controls — confirmed option 6B:** regenerate requests, accepted native labels, normalization, branch-specific native anchors, validation and a new untouched holdout. Labels come from the classical initializer only. Do not use mapped old profiles, old weights, neural-generated initial guesses, neural-assisted salvage, or raw predictions as labels. Use deterministic new seeds, canonical-hash deduplication, input-defined folds (guide stage-count rule), TRAIN-only normalization and root-disagreement quarantine. Keep failures/open-set cases. Register finite design size, request budgets, three training seeds and checkpoint endpoints; no unbounded acquisition loop. If classical-only coverage cannot produce a qualifying learned model, report the gap and keep classical available rather than silently changing the approved acquisition policy.

**Training and qualification:** register cohorts, architecture, seeds, checkpoints, budgets and selection rules before measurements. Use guide deterministic float32 CUDA/TF32-off setup, three scratch seeds, and isolated ten-worker/30s-request/2s-neural/16-base-iteration campaigns. Do not run campaigns concurrently with tests. Select by strict native convergence, not offline profile error. Export float64 parity fixtures and compare Java/Python values, decoded zero masks, branches, cancellation and shared-model execution.

Measure`CURRENT_ONLY`,`LNN_ONLY`,`LNN_FIRST` through the production reader on the frozen diagnostic panel and full validation, with two reversed-order blocks. Preserve the union of contemporary classical successes, require replicated strict-count benefit and the registered latency gate versus the new-basis classical control/compatible incumbent. Freeze the candidate before opening the fresh holdout. If no candidate passes, keep numerical initialization available and report neural promotion incomplete.

Qualify one new regrouped-basis artifact first; delete obsolete weights. Section 7 must subsequently support multiple separately qualified models, with this artifact as the first entry. Update scientific fingerprints, artifact path, model card, SHA/parameter/dimension pins, decoded-seed fixtures, exposure/provenance manifests and `.gitattributes` byte preservation. Regenerate old production-dependent neural test fixtures; retain independent algorithmic fixtures when still meaningful. Run the production-path campaign after packaging, not only a Python/offline evaluation.

Artifact packaging is set before stage-5 qualification: the immutable SHA-pinned payload contains weights, normalization, encoding revision and component axis. A sidecar owns package/sub-basis eligibility, coverage, decoder/candidate/correction rules and the payload hash. Stage 7 can replace eligibility metadata without rewriting trained bytes, but the full sidecar+payload pipeline identity must still be requalified. No old artifact-format compatibility reader is required.

No-promotion branch: retain the strict-convergence improvement criterion; latency improvement alone with equal strict counts is not sufficient under this registered plan. If no candidate qualifies, section 7 still implements and tests the registry using fixture models; the production registry has no qualified learned entry and falls back to classical. Report neural promotion as incomplete, never as successful retraining deployment. Continuing other work does not require promoting a failed candidate or violating classical-only label generation.

Preflight the local tooling: this checkout conditionally loads`tools/development.gradle` (provides`generalNeuralExperiment`), and the guide references predecessor files in another worktree. Reuse code patterns, not old numerical data/weights. Verify JDK21, CUDA and Python dependencies and every path before an expensive run. Write the new campaign beneath`research/crude-regrouping/training/`; only the final deployed artifact and minimal automated-test fixtures belong in tracked resources.

Tooling: use `C:/Users/wormz/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe` for existing research checks; the WindowsApps `python` stub is not a runtime. Training needs a verified CUDA environment built from `tools/neural/requirements-transformer.txt`; do not assume `.neural-venv` exists. Record actual interpreter/library/GPU versions before the registered run.

## 6. Initial column and pipeline qualification

Run sequentially after training has stopped:

```
./gradlew.bat test --offline
./gradlew.bat fluidScienceTest fluidRuntimeTest --offline
./gradlew.bat runFluidGameTestServer --offline
```

Confirm task names/discovery against the checkout and use the existing isolated GameTest run-directory option. A missing fixture, skipped campaign or missing server is not a pass. Capture all reports under this research collection.

Column checks: every preset with classical and learned-first settings; accepted results/branch/water certificates; columns with draws/pumparounds/steam; neural fallback outside its envelope; serialized state and multiplayer names; stale results after reload; snapshot consistency and malformed-reload rollback; GUI/API component order and temperature entry.

Pipeline checks: all existing crude generator presets (currently Tia Juana, WTI and Cold Lake) at 293.15 K, 298.15 K and heated states, reservoirs, pipes, pumps, control valves, fill/drain and mixed-crude networks; nitrogen/water and two-phase behavior where supported. Exercise Dalia/Bonga/Upper Zakum through programmatic feeds for transport qualification; adding their generator presets is not implicit in "all existing presets". Assert mass/energy conservation, finite pressure drop/flow, no negative inventory, convergence as viscosity grows, and no finite-viscosity 'solid blockage' shortcut. Verify reload invalidates cached properties while admitted jobs finish against their snapshot. Test current-basis save/reopen; old saves are unsupported, not a migration acceptance target.

Finish in Minecraft with actual generator-to-pipe/pump/reservoir paths and calculator solves; exercise a datapack override plus`/reload`, and verify the new learned model is reported for eligible requests. Record unavailable coverage honestly rather than substituting unit-test success for gameplay evidence.

## 7. Extensibility framework last, then final verification

Confirmed option 5A with the user's ordering: complete regrouping, classical qualification, initial training and section 6 before this work package. Do not redesign the whole framework as a prerequisite to the scientific change. Do not merge the overall release until the post-framework verification passes.

- Make component axes explicit and derive widths and special-species locations by ID throughout fluid state, GUI, packets, tests and training tools. Remove fixed 20/21/22 and nitrogen/water offsets. A current-format fingerprint/axis protects interpretation; **automatic migration of older saves remains out of scope**, including generic additive-save migration from the review.
- Add canonical per-property and ordered-sub-basis physics fingerprints, covering relevant interactions, water/caloric reference and domains as well as property records. Assay edits alone must not disqualify a model for the same actual feed and physics. Keep transport and whole-catalog identities where their consumers need them.
- Use one configurable network basis initially, with ID-based preset projection and checks that shared properties/interactions agree. Per-island adaptive bases are deferred. Bound packets and component counts, and qualify larger active bases rather than promising unlimited scale.
- With save migration explicitly excluded, future edits to that global network basis can again invalidate existing development worlds. "Ready for regular additions" means extensible code/data/model selection, not save-safe upgrades. Current-format ID axes leave room for a later migration feature without promising it now.
- Add a bundled neural registry and shape-derived reader. Each entry owns model ID, encoding revision, component axis, physics fingerprint, weights hash, coverage, decoder/candidate/correction rules and parity fixtures. Select deterministically by declared priority among eligible entries; no eligible model means classical fallback. Multiple composition specialists may overlap if priority is explicit. Gather/scatter by ID and require every nonzero feed component to be represented; zero padding is only allowed inside the model's qualified domain. This inference projection is not old-save compatibility.
- Make training/export/parity tools consume one basis descriptor per campaign. Existing qualified weights must reproduce their decoded outputs after the registry refactor. Use test models to prove multi-model routing; additional production specialists require their own qualified labels/training and are not fabricated just to fill the registry.
- Introduce reusable shared-basis records, ID-keyed sparse assay amounts, generated/validated material indexes, common component qualification tests, and a compound-addition checklist. Resolve these authoring conveniences to immutable whole records before publishing the catalog; specify override/cycle/reference rules explicitly. Move classpath-only fluid calibration and dissolved-solute transport into validated scientific data records as part of this last-stage extensibility work, preserving numerical behavior and measured outputs.
- Make presets data-driven and composition tables scrollable/localized for the admitted basis size. Keep existing preset operating behavior under regression tests. New compounds still require a suitable thermodynamic/transport model; no aqueous-solubility, NRTL, reaction or appended chemical-quality solver is added here.
- Keep model/catalog selection consistent for each admitted job. A reload must not change a running calculation's chosen scientific/model snapshot. Bundled-only models suffice initially; arbitrary datapack-supplied model weights are deferred.

Final gate: rerun section 6's science/runtime/serialization/GameTests and actual Minecraft column/pipeline checks after the framework changes, plus registry routing, rejected eligibility, zero-padding, packet-size and generic-component tests. Re-run production model parity and native comparison so a reader/registry change cannot pass on offline agreement alone. Demonstrate adding a qualified compound and a second fixture model without editing fixed array widths or a single-artifact constant. If a new-basis model was promoted, verify its qualification remains valid; if arithmetic changes, requalify before shipping. Otherwise verify the empty-qualified-registry/classical fallback and report the neural deliverable incomplete.

## 8. Delivery and stop conditions

- Active material/transport resources, presets and names use the new basis everywhere; no obsolete production dataset or neural artifact remains.
- Classical qualification is complete before neural promotion. A promoted transformer must pass parity, native gates and packaged production-path checks and be served through the final registry. Without promotion, the numerical/framework work may be qualified separately, but the requested neural deployment remains incomplete and must be reported as such.
- Full science/runtime/serialization and discovered GameTests pass; final column and pipeline gameplay checks are recorded.
- Research articles, source extracts, plots, training corpora/checkpoints and reports stay under ignored`research/`. Production changes and necessary independent test fixtures are committed, per the standing instruction; do not force-add research files.
- No elemental/SARA append task is started. The retained documents provide the later design context.

Do not report completion merely because JSON loads, a model trains, or a column produces an advisory result. Each gate needs its own recorded outcome. If characterization, classical convergence or model promotion fails, fix that stage or report its concrete blocker; do not weaken its tolerance or silently retain the old basis.
