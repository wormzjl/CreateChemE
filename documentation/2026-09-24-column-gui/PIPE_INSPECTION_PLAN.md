# Automatic pipe inspection plan

In progress since 2026-09-24, same column GUI batch/worktree.

Remove Forward/Reverse/Run selection. Display the interval's transported contents automatically. A straight section combines gross forward/reverse history, with an explicit reversal notice; a physical junction sums outgoing transport once to avoid doubling throughput. Retain all connection metrics and expose a read-only connections table for branching equipment.

Use the freed left operating panel for flow rate, bulk velocity, mean pressure difference per metre, phase volume ratios and wrapped status/errors. Remove the full-width pipe status above the contents. Keep the shared slate/aqua instrument palette and native font, table alignment and compact inputs. Hover text explains ranges, last-interval averages and pressure gradient including elevation.

Calculate display metrics from accepted graph/history at the scheduled server presentation only. Do not run thermodynamics or alter physics. Bump fluid protocol for new pipe inspection metadata and validate it. Test physically compiled 4/5/6-way junctions, flow and component conservation, reversal handling and no throughput double counting; verify layouts in a fresh dev world and leave client open.
