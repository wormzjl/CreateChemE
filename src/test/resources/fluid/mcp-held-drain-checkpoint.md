# MCP drain regression provenance

Captured from the isolated `Fluid M8 MCP` world on 2026-09-16, before adding velocity clamping. The source was the gzip-compressed `run/mcp-client/saves/Fluid M8 MCP/data/createcheme_fluid_core.dat`; its checkpoint SHA-256 was verified before extraction. Only island 13 was retained. This island had no module transfers; unrelated islands were excluded and the empty module/transfer envelope was normalized.

The fixture retains the exact canonical inventories, energy, phase guesses, historical flow, clocks, fallback state and old `Unsupported sonic flow` status. It is generated gameplay data, not a thermodynamic reference oracle. `McpGameplayRegressionTest` verifies that the new bounded hydraulic law advances it without changing the EOS/flash or losing components/energy. Keep the pre-clamp checkpoint unchanged for reproducibility.

`mcp-clamped-liquid-full-checkpoint.json` is the subsequent saved island after catch-up reached tick 26787. It contained approximately 0.000247151 mol nitrogen and 55289.207945 mol water in 1 m³. The first clamp build then exhausted substeps because Newton residuals stalled just above its former internal 1e-11 target. The fixture preserves that failure and its clocks. The revised internal 1e-10 target remains stricter than the unchanged full-equation 1e-8 gate; the replay additionally compares pressure, temperature, inventory and integrated pipe rates with smaller-step, tighter-time-tolerance step doubling.

Continuing in Minecraft exposed a reconstruction-closure failure four simulated seconds later. Bulk-normalized component rows did not resolve the tiny nitrogen inventory accurately enough relative to approximately 55,289 mol of water. Nearly liquid-filled layouts now scale real component traces and finite-difference perturbations against their own amounts. Artificial entry seeds for absent components retain their previous bulk scale; no canonical amount is removed or invented. The regression now continues for **1,000 simulated seconds** after the initial refined-reference comparison and requires nitrogen to remain positive. This is a deterministic numerical replay, not a paced performance benchmark or soak.
