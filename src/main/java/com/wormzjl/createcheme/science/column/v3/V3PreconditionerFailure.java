package com.wormzjl.createcheme.science.column.v3;

/** Stable internal non-success causes for sequential preconditioner attempts. */
enum V3PreconditionerFailure {
    PROPERTY_DOMAIN,
    INVALID_STATE
}
