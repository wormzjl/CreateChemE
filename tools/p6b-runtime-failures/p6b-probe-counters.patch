diff --git a/src/main/java/com/wormzjl/createcheme/science/fluid/network/PassiveIntervalSolver.java b/src/main/java/com/wormzjl/createcheme/science/fluid/network/PassiveIntervalSolver.java
index e322e99..c5f4951 100644
--- a/src/main/java/com/wormzjl/createcheme/science/fluid/network/PassiveIntervalSolver.java
+++ b/src/main/java/com/wormzjl/createcheme/science/fluid/network/PassiveIntervalSolver.java
@@ -53,7 +53,7 @@ public final class PassiveIntervalSolver {
     public Result solveApproximate(PassiveNetwork initial,double duration,Settings settings,Runnable checkpoint,TrBdf2StepSolver.StageGuard guard) {
         if(com.wormzjl.createcheme.science.fluid.transport.SolidMobility.requiresFull(model,initial))throw new ApproximationRejected("Solid/filter intervals require a full solve");
         // Deposition and sublimation are the engine's UV answers after each accepted full substep (P4 stage 2b).
-        if(model.crystalsCompete())throw new ApproximationRejected("Crystal equilibria require a full solve");
+        if(model.crystalsCompete()&&!Boolean.getBoolean("p6b.approximateCrystals"))throw new ApproximationRejected("Crystal equilibria require a full solve");
         TrBdf2StepSolver.StageGuard checked=(states,modes)->{
             for(var state:states)if(state.solidMoments().mass()>0||com.wormzjl.createcheme.science.fluid.transport.SolidMobility.immobileLiquid(model,state))throw new ApproximationRejected("Solid transition requires a full solve");
             guard.check(states,modes);
@@ -131,7 +131,7 @@ public final class PassiveIntervalSolver {
                     SolverDiagnostics.attempt(attempt,step,error<=settings.relativeTolerance,
                             sourceError>=Math.max(stateError,pipeError)?"boundary":pipeError>=stateError?"pipe":"state",error);
                     if(error>settings.relativeTolerance)throw new AccuracyRejection("Embedded "+(sourceError>=Math.max(stateError,pipeError)?"boundary":pipeError>=stateError?"pipe":"state")+" error "+error,error);
-                    accepted=crystals(replace(accepted,full),acceptance,checkpoint);last=full;elapsed+=step;acceptedCount++;consecutiveRejects=0;
+                    accepted=Boolean.getBoolean("p6b.crystalsAtEnd")&&elapsed+step<duration?replace(accepted,full):crystals(replace(accepted,full),acceptance,checkpoint);last=full;elapsed+=step;acceptedCount++;consecutiveRejects=0;
                     work+=full.pumpWorkJoule();boundaries.addAll(full.boundaries());var flows=full.massFlows();
                     pipeTransfers.add(full.pipeTransfers(),1);
                     for(int i=0;i<transferred.length;i++){transferred[i]+=step*flows[i];grossTransferred[i]+=step*Math.abs(flows[i]);}
@@ -148,7 +148,7 @@ public final class PassiveIntervalSolver {
                 if(!initial.scheduledTransfers().isEmpty())error=Math.max(error,boundaryError(full,first,second)/(regimes.smooth()?3:1));
                 SolverDiagnostics.attempt(attempt,step,error<=settings.relativeTolerance,"refinement",error);
                 if(error>settings.relativeTolerance)throw new AccuracyRejection("Step refinement error "+error,error);
-                accepted=crystals(replace(accepted,second),acceptance,checkpoint);last=second;elapsed+=step;acceptedCount+=2;consecutiveRejects=0;
+                accepted=Boolean.getBoolean("p6b.crystalsAtEnd")&&elapsed+step<duration?replace(accepted,second):crystals(replace(accepted,second),acceptance,checkpoint);last=second;elapsed+=step;acceptedCount+=2;consecutiveRejects=0;
                 work+=first.pumpWorkJoule()+second.pumpWorkJoule();boundaries.addAll(first.boundaries());boundaries.addAll(second.boundaries());
                 pipeTransfers.add(first.pipeTransfers(),1);pipeTransfers.add(second.pipeTransfers(),1);
                 var q1=first.massFlows();var q2=second.massFlows();for(int i=0;i<transferred.length;i++){transferred[i]+=step*.5*(q1[i]+q2[i]);grossTransferred[i]+=step*.5*(Math.abs(q1[i])+Math.abs(q2[i]));}
diff --git a/src/main/java/com/wormzjl/createcheme/science/fluid/thermo/FluidThermodynamics.java b/src/main/java/com/wormzjl/createcheme/science/fluid/thermo/FluidThermodynamics.java
index 23012df..910b6f4 100644
--- a/src/main/java/com/wormzjl/createcheme/science/fluid/thermo/FluidThermodynamics.java
+++ b/src/main/java/com/wormzjl/createcheme/science/fluid/thermo/FluidThermodynamics.java
@@ -412,6 +412,7 @@ public final class FluidThermodynamics {
         boolean crystals=crystalsCompete(t,overall);
         var result=phaseEngine().tp(t,p,overall,crystals);
         for(int evaluation=result.diagnostics().kernelEvaluations();evaluation>0;evaluation--)checkpoint.run();
+        P6B_TP_CALLS.increment();P6B_TP_KERNEL.add(result.diagnostics().kernelEvaluations());
         if(crystals&&node!=null&&result.converged()&&holdsCrystal(result)&&sum(node.liquidView())>0) {
             // P5: the outer check of a node that carries a liquid, whose crystal inventory is frozen over the step: its mobile
             // fluid takes its own split (the crystals held out), which keeps a node on a binary's three-phase line in its
@@ -612,10 +613,12 @@ public final class FluidThermodynamics {
      * ceiling the fluid-only answer (every crystal sublimes). {@code checkpoint} runs once before each engine call and once
      * per kernel evaluation it reports.
      */
+    public static final java.util.concurrent.atomic.LongAdder P6B_UV_CALLS=new java.util.concurrent.atomic.LongAdder(),P6B_UV_KERNEL=new java.util.concurrent.atomic.LongAdder(),P6B_UV_FAILED=new java.util.concurrent.atomic.LongAdder(),P6B_TP_CALLS=new java.util.concurrent.atomic.LongAdder(),P6B_TP_KERNEL=new java.util.concurrent.atomic.LongAdder();
     private EquilibriumResult crystalUv(double energy,double freeVolume,double[] totals,boolean wet,boolean warm,double t,double p,Runnable checkpoint) {
         checkpoint.run();
         var result=warm?phaseEngine().uv(energy,freeVolume,totals,true,t,p):phaseEngine().uv(energy,freeVolume,totals,!wet);
         for(int evaluation=result.diagnostics().kernelEvaluations();evaluation>0;evaluation--)checkpoint.run();
+        P6B_UV_CALLS.increment();P6B_UV_KERNEL.add(result.diagnostics().kernelEvaluations());if(result.status()!=EquilibriumResult.Status.CONVERGED)P6B_UV_FAILED.increment();
         // Above the crystals' ceiling no crystal is stable: the fluid-only answer (every crystal sublimes).
         if(!wet&&result.status()==EquilibriumResult.Status.OUT_OF_DOMAIN&&result.violation().property()==ThermoDomainViolation.Property.TEMPERATURE
                 &&result.violation().value()>result.violation().maximum()&&result.violation().maximum()<=crystalCeiling) {
diff --git a/src/main/java/com/wormzjl/createcheme/science/thermo/phase/FluidTpEquilibrium.java b/src/main/java/com/wormzjl/createcheme/science/thermo/phase/FluidTpEquilibrium.java
index 692d959..b217018 100644
--- a/src/main/java/com/wormzjl/createcheme/science/thermo/phase/FluidTpEquilibrium.java
+++ b/src/main/java/com/wormzjl/createcheme/science/thermo/phase/FluidTpEquilibrium.java
@@ -233,7 +233,7 @@ public final class FluidTpEquilibrium implements EquilibriumService {
      * methane flush (row E), warm answers 55/55, 166/166, 180/211 per interval at 16 against 43/55, 93/166, 2/211 at 8 (154
      * of 211 at 12, none more at 24).
      */
-    public static final int WARM_UV_EVALUATIONS = 16;
+    public static final int WARM_UV_EVALUATIONS = Integer.getInteger("p6b.warmBudget", 16);
     private static final int LINE_SEARCH_HALVINGS = 4;
     private static final int COEXISTENCE_ITERATIONS = 100;
     /** The nested UV search's loosest volume solve, in {@code ln V}, while the energy is still far off. */
@@ -1451,11 +1451,13 @@ public final class FluidTpEquilibrium implements EquilibriumService {
 
     // UV.
 
+    public static final java.util.concurrent.atomic.LongAdder P6B_WARM_ATTEMPTS=new java.util.concurrent.atomic.LongAdder(),P6B_WARM_HITS=new java.util.concurrent.atomic.LongAdder();
     private EquilibriumResult solveUv(double u, double v, double hintT, double hintP, PhaseCompetition competition, Workspace w) {
         if (hintT >= w.minimumT && hintT <= w.maximumT && hintP >= w.minimumP && hintP <= w.maximumP) {
             // P6 warm start: the Newton phase from the previous answer, on a short budget; the cold search otherwise.
+            P6B_WARM_ATTEMPTS.increment();
             EquilibriumResult warm = newtonUv(hintT, hintP, null, u, v, competition, w, WARM_UV_EVALUATIONS);
-            if (warm != null) return warm;
+            if (warm != null) {P6B_WARM_HITS.increment();return warm;}
             w.outerEvaluations = 0;
             w.jacobianValid = false;
             w.bestT = Double.NaN;
