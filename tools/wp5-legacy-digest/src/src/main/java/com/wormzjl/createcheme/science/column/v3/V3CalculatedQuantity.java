package com.wormzjl.createcheme.science.column.v3;

/** A V3.0 quantity reported by a successful solve rather than supplied as a column control. */
public enum V3CalculatedQuantity {
    CONDENSER_DUTY,
    EXTERNAL_OVERHEAD_COMPONENT_FLOWS,
    BOTTOMS_COMPONENT_FLOWS,
    STAGE_LIQUID_COMPONENT_FLOWS,
    STAGE_VAPOR_COMPONENT_FLOWS
}
