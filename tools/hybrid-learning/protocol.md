# Native baseline / residual Transformer study, revision 1

This additive offline study follows the user-authorized salvage, training and comparison task of 2026-09-11. Production equations, thermodynamics, budgets, solver strategy, acceptance gates, incumbent weights and prior sealed sources remain fixed.

N consists of the exact original 805 eligible strict TRAIN records. N+1 appends only finalized novel strict TRAIN labels from the complete ten-worker frozen-transformer acquisition sweep. Original validation/test labels are never upgraded. Dataset consumption requires the execution validator, unchanged N prefix and nontraining rows, canonical uniqueness, original split membership and native same-input provenance. Alternative-root conflicts follow the acquisition's preregistered quarantine rule.

## Fixed architecture

An input-only global head chooses a legal observed condenser branch, in training, validation and native inference. Its branch-conditioned native MATERIAL_CLOSED initializer supplies a numeric anchor, not a qualified physical solution. Steam and side draws remain authored; this baseline does not claim their equations or energy balance are closed. Baseline construction failure uses zero coordinates plus an explicit unavailable flag and retains the sample. Cancellation propagates. Native inference reconstructs the anchor each request inside the two-second neural budget.

The network has 185 node inputs (96 original, 85 normalized anchor coordinates, three branch indicators, one availability indicator), two transformer layers of width 64, four heads, FFN width 128 and 85 outputs: 89,496 parameters. The global branch head retains 74 inputs. Residual outputs reconstruct the first 43 encoded coordinates: temperature, two log phase totals and two sets of 20 centered composition coordinates. Water, wet and phase-presence coordinates are absolute learned outputs. The unchanged factorized decoder preserves fixed condenser temperature, structural/inactive zeros, trace handling and phase-total limits. No thermodynamic parameters are learned. The exact frozen one-pass material completion wrapper follows decoding; ordinary decline retains this hybrid prediction. The incumbent plus that same wrapper is an independent control.

The native bank is input-only. TRAIN and original certified validation profiles are the only labels loaded by the trainer. Baseline, input, residual and target normalization are computed once from N, with each column equally weighted; N+1 reuses those exact bytes. Residual normalization uses the label-branch anchor to establish coordinate scales, but minibatch reconstruction always uses the classifier-selected branch. Legal-branch masks follow reflux constraints and actual training class coverage.

## Fixed training

Six fits: N and N+1 paired seeds 20260910, 20260911 and 20260912. Same initial weights per seed, verified by hashes; AdamW lr 0.0008, decay 0.0001, dropout zero, batch 32, clip 1, float32/TF32 off. Exactly 4,160 optimizer updates per fit; reshuffle full data passes and retain the final short minibatch. Validate every 26 updates; no early stopping. Choose the lowest original decoded selection score on the unchanged 168 certified validation references, earliest checkpoint breaking ties. Record presentations and passes because equal updates do not mean equal epoch coverage. No extra loss, augmentation, reweighting or architecture search.

## Prospective evaluation

Freeze an input-only fresh pool using generator seed 202609116 and selector seed hybrid-residual-20260911-v1 before fitting/native validation. Exclude all registered historical input hashes and candidate pools, including the last checkpoint-selection pool. Select 252 inputs: four per stage count 2..64, two with steam and two without. No replacement sampling.

Evaluate incumbent, exact incumbent wrapper and all six hybrid exports on all 405 original validation inputs. Use two complete blocks with deterministic reversed pipeline order and balanced mode order; one owned ten-worker campaign at a time, no GPU fitting or competing numerical campaign. Every pipeline receives CURRENT_ONLY, LNN_ONLY and LNN_FIRST. Same original TRAIN warmup, Java 21, 4 GiB heap, 30-second request deadline, 2-second neural budget and 16 correction iterations. Entire inference including native anchor, decoding and completion is charged. Diagnostics are separate from measured requests. Report complete request latency, failures, fallback and qualification; repeated blocks do not enlarge the independent-input count.

Select one seed per dataset by higher minimum LNN_FIRST strict count over both validation blocks, then pooled all-case mean latency, then seed. A replacement recommendation additionally requires strict-count improvement over incumbent in both blocks, preservation of the union of contemporaneous classical successes in both blocks and no pooled mean-latency regression. Also disclose incumbent-only neural losses. Freeze the decision and selected pipelines before testing incumbent, wrapper and both selected hybrids on the fresh 252 inputs, using two blocks and the identical policy. No test-driven refitting, runner-up selection or promotion.

## Verification and scope of conclusions

Require Java/Python parity for anchors, reconstruction and decoded branches/masks/flows; shared-model parallel repeatability, cancellation and worker shutdown; dataset isolation and normalized-statistic provenance. Numerical anchors and raw error scores never replace native correction and strict audit. Results cover the acquired distribution, with stage/steam/draw/heat strata and old-N/new-label coverage separately reported. Comparison to incumbent changes both representation and training history and is not an isolated architecture ablation.

All source hashes and data hashes are frozen before dependent fitting or campaigns. Retain complete journals, run policies, initial preflight failures, six checkpoints, selection, reports and archive verification. The organization audit preserves 71 identical misplaced cache duplicates relocated from main. One serial input-only anchor export (973 inputs, approximately 18 seconds) overlapped the end of acquisition; no corrected-column benchmark or model fitting overlapped that sweep, and acquisition timing is not a comparative speed claim.
