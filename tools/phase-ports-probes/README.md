# phase-ports-probes

Measurement drivers and gate logs of the batch `documentation/2026-09-26-phase-ports-and-compressor/`, one subfolder per work package. Nothing here is run by a Gradle task; the gates of record are the Gradle commands recorded in `PHASE_PORTS_REVIEW.md` of that batch. Written in the cloud container (OpenJDK 21.0.10, Linux); a Windows JDK 21.0.11 run differs in one ledger roundoff value (see the review, WP1 section 6).

## wp1/ (commit `35e354d`, review section 6-8 and 11)

- `src/BitwiseProbe.java`: gate G0b. Replays chain-100 and 14 all-BULK scenarios (mixed-gas junctions at 5 s and 0.1 s, rising/falling water lines, a pumped line, a wet-crude drain, a slurry filter, a valve) and dumps every double in hex; run on the base and on WP1, the outputs are byte-identical (`results/probe/out-base/`, `results/probe/out-new/`; `scenarios.txt` is the per-scenario dump, `chain-100.json` the regression encoding, byte-identical to `src/test/resources/fluid/regression/chain-100.json`).
- `src/JunctionProbe.java` and `src/instr/PassiveStepSolver.java`: the instrumented scratch copy of the reconstruction's equation gate used to locate the base defect of review section 8 (a junction seeded dry receiving unsaturated water vapour). The instrumented file is a copy of `PassiveStepSolver.java` at `6e1c5b6` with print statements; never build the mod with it.
- `src/HumidJunctionProbe.java`: reproduces that defect on `6e1c5b6` and on WP1 (identical gate residuals 1.3058e-8 at 0.5 % water, 2.6151e-8 at 1 %).
- `src/science-build.sh`: the first javac-only harness (science package and its Minecraft-free tests) used before Gradle could resolve dependencies in the container; superseded by `tools/cloud-science-harness/`.
- `logs/`: the WP1 gate logs, numbered as in the review: `01` cloud harness, `02` Gradle fluid suites on the clean base worktree (`6e1c5b6`; `02-junction-lines-base.txt` holds the 33 MIXED_GAS/LIQUID_JUNCTION lines, `02-test-names-base.txt` the 406 names), `03` the same on WP1 (413 tests, `03-junction-lines-wp1.txt` identical apart from ms/bytes), `04` exact regression 0.000e+00, `05` the 38 adjacent, `06` GameTest and mcpCompat compile, `07` javac science harness.

How to run a probe: compile the science sources (or use `tools/cloud-science-harness/harness.sh compile`) and `javac`/`java` the probe against that output with gson and EJML on the class path, as `science-build.sh` does; each probe prints where it writes its outputs.

## wp2/ (commit `d836cf2`, review WP2 sections 3-6 and 12)

- `src/PhasePortDutyCycleProbe.java`: risk R1. A LIQUID port that drains far faster than its vessel is fed (1 m3 N2 tank at 150 kPa, 2 % water, fed through a 4 mm line from a 200 kPa water generator, drained through a 25 mm LIQUID line to a void); counts open/closed transitions at 0.1 s and 5 s slices and at fixed 1 s steps (`logs/09-duty-cycle-probe.txt`: 45 / 9 / 5 transitions in 300 s). A JUnit class: copy it into `src/test/java/com/wormzjl/createcheme/science/fluid/network/`, run `tools/cloud-science-harness/harness.sh compile` then `select --select-class=com.wormzjl.createcheme.science.fluid.network.PhasePortDutyCycleProbe`, and delete the copy.
- `src/SqueezeGateProbe.java`: the squeeze fixture with BULK or phase ends (a method to paste into `PhasePortClosureTest`), classifying the VAPOR-vent equation-gate rejections as WP1 behaviour (`logs/12-*`: 2 on WP2, 4 on the WP1 tree `3dbd9b8`, 0 with a BULK vent). For the WP1 tree, `git archive 3dbd9b8 src | tar -x -C <dir>`, replace `PassiveStepSolver.portPhaseShare`/`PHASE_PORT_*` in the copied test with local equivalents, and run the harness with `REPO=<dir>`.
- `src/counterfactual-reopen-no-mask.patch` (against `d836cf2`): the reopen test reading an all-zero mask; `logs/10-*` show 20 (5 s) and 322 (0.1 s) spurious reopens on `thePortAloneRefusesOutflow...`, against 1 and 1. `src/gate-instrumentation.patch`: prints the gate's worst row (`logs/11-*`: the tank's local row 4, 4.45e-8 and 2.06e-7). Apply with `git apply`, measure, `git checkout` the file; never commit either.
- `logs/`: `wp2-01` cloud harness all, `wp2-02` Gradle fluid suites (428 tests; `-junction-lines.txt` identical to `wp1/logs/02-junction-lines-base.txt` with ms/bytes masked; `-test-names.txt`), `wp2-03` exact regression 0.000e+00, `wp2-04` the 38 adjacent, `wp2-05` GameTest and mcpCompat compile, `wp2-06`/`wp2-07` the G1/G2 re-run on the final tree, `08` the harness run of `PhasePortClosureTest` with every slice line.

## d10/ (commit `07e7426`, review section "D10")

- `src/trace-sites.patch` (against `07e7426`): prints the calling test each time a junction gets the water trace; applied to the harness science, runtime and adjacent runs it fires only in `JunctionWaterTraceTest` (`logs/07-trace-sites.txt`). Apply with `git apply`, run, `git checkout` the file; never commit it.
- The humid-junction reproduction is `wp1/src/HumidJunctionProbe.java` (compile it against the harness's main classes plus `src/main/resources`, gson and EJML): `logs/06-humid-probe-d10.txt` all six cases integrate, `logs/06-humid-probe-wp2.txt` the 0.5 % and 1 % cases fail as before.
- `logs/`: `01` cloud harness all, `02` Gradle fluid suites (430 tests; **`02-junction-lines-d10.txt` is the junction-line base for WP3**, identical to `wp1/logs/02-junction-lines-base.txt`), `03` exact regression 0.000e+00, `04` the 38 adjacent, `05` GameTest and mcpCompat compile, `08` the WP1 bitwise probe's scenario dumps on the WP2 and D10 trees (byte-identical).

## d9/ (commit `fdf3574`, review section "D9")

- `src/BitwiseProbe.java`: the WP1 probe (gate G0b) extended with `gas-ports.txt`, eight gas-only islands whose vessels carry LIQUID and VAPOR ports (a N2 generator into a LIQUID port, a VAPOR-to-LIQUID tank pair and a methane/nitrogen junction fed through phase ports, each at 5 s and 0.1 s; a dead-headed rising LIQUID port; a dry LIQUID drain), which the head must leave bitwise, and `liquid-ports.txt`, a rising line and a drain whose LIQUID port sees water (expected to move; not a gate). Run on `0cf4ec9` (WP2 + D10, before the edit) and on the D9 tree: `results/probe/out-wp2/`, `results/probe/out-d9/`, `results/probe/scenarios.txt` (identical on both), `results/probe/sha256.txt` (every file of both runs plus the regression reference; `chain-100.json`, `scenarios.txt`, `gas-ports.txt` identical). Compile and run as the wp1 probe: `javac -d <out> -cp <harness OUT>/main:<gson, EJML> BitwiseProbe.java`, `java -cp <out>:<harness OUT>/main:src/main/resources:src/generated/resources:<jars> BitwiseProbe <output dir>`.
- `src/DrainProbe.java`, `src/ManometerProbe.java` (package `com.wormzjl.createcheme.science.fluid.network`, compile against the harness's main classes as above; arguments in each file's javadoc): the classification of the equation-gate rejections met by the D9 fixtures. `logs/08-vapor-vent-gate-classification.txt`: a VAPOR vent on a tank filled through its LIQUID port exhausts the substep limit on the WP2 + D10 tree as on D9 (WP2 option 3 class); `logs/08-drain-gate-classification.txt`: the head drain on D9 against the WP2 tree with the head emulated by a lower void.
- `src/mutations/no-head-in-{closeDeadHeads,reopenable,edgeRows}.patch` (against `fdf3574`): each removes the head from one site; `logs/09-mutation-*.txt` show which `LevelHeadTest` cases catch it (3, 1 and 7 of 8 fail). Apply to a copy of the tree (`git apply`), run the harness `select --select-class=...LevelHeadTest` with `REPO=<copy>`; never commit them.
- `logs/`: `01` cloud harness all (213 + 237 + 38 + chain-100), `02` Gradle fluid suites (448 tests; `02-junction-lines-d9.txt` identical to `d10/logs/02-junction-lines-d10.txt`; `02-test-names-d9.txt`), `03` exact regression 0.000e+00, `04` the 38 adjacent, `05` GameTest and mcpCompat compile, `06` the `LEVEL_HEAD_*` and phase-port summary lines of G1, `07` the phase-port summary lines on the WP2 + D10 tree (for the moved numbers), `08` gate classification, `09` mutations, `10`/`11` the G1/G2 re-run on the final tree.

## extreme/ (commit `55508e4`, `documentation/2026-09-26-phase-ports-and-compressor/EXTREME_TOPOLOGY_TESTS.md`)

- `src/ExtremeTopologyScanProbe.java`: the temporary scan test method (kept out of `src/test`) that located the pass/fail boundary of the owner's two extreme block layouts (`ExtremeTopologyIslandTest`): column count, pressure spread and velocity cap, plus the water variant. Paste it into the test class to rerun; it needs the same builders.

## vent/ (investigation on `fdc7571`, review section "Vent gate defect"; no tracked source changed)

- `src/instr-gate-probe.patch` (against `fdc7571`; `PassiveStepSolver` and `SparseNewton`): at a failing equation gate prints every row of the Newton's point and of the reconstructed point, each node's amounts against the Newton's targets and the reconstruction's inventories, and each phase-port edge's booking by the Newton and by the reconstruction per component (`-Dgateprobe.max=N`, default 3); `-Dgateprobe.compact` one line per reconstruction (with `-Dgateprobe.all` also the passing ones); `-Dnewtonprobe` the Newton's norm and rows 0 and 6 per iteration; `-Dnewton.freshStart` / `-Dnewton.fresh` a Jacobian at every solve start / every iteration. Apply to a copy of the tree (`git archive fdc7571 src | tar -x -C <dir>`, `git apply`), compile with `tools/cloud-science-harness/harness.sh compile` (`REPO=<dir> OUT=<out>`), run the d9 probes against `<out>/main` with `src/run-probe.sh`; never commit it.
- `src/vent-gate-polish-prototype.patch` (against `fdc7571`): option A of the review, a re-solve on a fresh Jacobian at the tolerance times 1e-2 when the FULL gate has failed, gated again before the step is refused; `-Dvent.polish.trace=true` prints each firing with the calling test. The prototype for the owner's decision; D13 (owner: option A) took it and commit `4a71629` ports it without the trace line, as `PassiveStepSolver.polishRefusedPoint`, applied to both acceptances.
- `src/run-probe.sh`: runs `d9/src/ManometerProbe.java` (default) or `P=DrainProbe` with `MAIN`, `PROBE`, `LIB`, `JOPTS` (usage in the file).
- `logs/`: `01` the manometer at 5 s on `fdc7571` (fails in slice 1 at 1.0083e-8); `02` the first three gate failures in full (instrumented); `03` compact runs of the four link/vent combinations; `04` Newton iterations (chord, LIQUID and BULK link); `05` the manometer with a Jacobian at every solve start / every iteration; `06` the manometer at 5 s with the prototype; `07` at 0.1 s on HEAD (the prototype's output is byte-identical); `08` the BULK-vent control on HEAD and prototype; `09` the D9 drain on HEAD, instrumented and prototype; `10` cloud harness `all` on the prototype (summary) and its firings per test; `11` the all-BULK runtime fixtures' gate failures (instrumented); `12` `LevelHeadTest` summary lines, HEAD against the prototype.
- D13 (commit `4a71629`, review section "D13"): `results/bitwise-probe/` holds the d9 `BitwiseProbe` outputs on `99f5312` (src identical to `fdc7571`) and on `4a71629` (`out-99f5312/`, `out-4a71629/`) with `sha256.txt`: all four files byte-identical, and identical to the d9 capture `d9/results/probe/out-d9/`; `chain-100.json` identical to the regression reference. `logs/13` cloud harness `all` on `4a71629` (summary), `logs/14` polish firings per suite (a traced copy of `4a71629`, not committed), `logs/15` the `LEVEL_HEAD_RUN` lines before and after plus the `VENT_GATE_RUN` lines of `VentGatePolishTest`.

## d11/ (commit `548a9bf`, review section "D11")

- `src/JunctionPortProbe.java`, `src/ColdVentProbe.java`, `src/HumidVentProbe.java` (package `com.wormzjl.createcheme.science.fluid.network`): the classification probes of D11. `JunctionPortProbe` runs the D9 probe's methane/nitrogen junction with every port combination (`logs/07`: VAPOR-BULK and VAPOR-VAPOR fail on HEAD `0795215` with "Negative transport reconstruction", the WP1 unbacked-trace defect D11 exposed; all pass on D11). `HumidVentProbe` vents a humid nitrogen tank through a BULK end from 200, 180 and 130 kPa (`logs/08`: the adiabatic expansion takes it below the water domain from 180 kPa on HEAD and on D11 alike, a base limitation). `ColdVentProbe` drains 0.1 m3 of water under nitrogen from 200 and 120 kPa through a BULK end and a LIQUID port (`logs/09`).
- `src/DrainTraceProbe.java`, `src/OverflowTraceProbe.java`: print every active-set pass (segments, capacities, phase codes, converged flows, re-decisions) of chosen slices of the drain and overflow fixtures; they need `src/pass-trace.patch` (against `548a9bf`: a `D11TRACE` switch in `PassiveStepSolver`). Apply it to a copy of the tree (`git apply`), never commit it.
- `src/compile-main.sh`: compiles a tree's main sources as the cloud harness does, without the tests (`REPO=<tree> OUT=<dir> LIB=<harness jars>`); compile a probe with `javac -d <out> -cp <OUT>/main:<gson, EJML> <probe>.java` and run it with `<out>:<OUT>/main:src/main/resources:src/generated/resources:<jars>` on the class path.
- `results/probe/`: the D9 bitwise probe (`../d9/src/BitwiseProbe.java`) on HEAD `0795215` before editing (`out-head/`) and on the D11 tree (`out-d11/`), `sha256.txt` (chain-100 and the 14 all-BULK scenarios byte-identical, chain-100 identical to the regression reference), `scenario-comparison.txt` (per scenario), `differing-gas-ports.txt` (the flows of the five gas-port scenarios in which a gas-only tank's LIQUID port now vents gas).
- `logs/`: `01` cloud harness all (208 + 237 + 38 + chain-100), `02` Gradle fluid suites (443 tests; `02-junction-lines-d11.txt` identical to `d10/logs/02-junction-lines-d10.txt`; `02-test-names-d11.txt`), `03` exact regression 0.000e+00, `04` the 38 adjacent, `05` GameTest and mcpCompat compile, `06` the fixture summary lines of G1 (`PHASE_PORT_PRIORITY_*`, `LEVEL_HEAD_RUN`, the WP1 and D10 fixtures), `07`-`09` the probes above.

## d12/ (code commit `418ca7e`, review section "D12", `EXTREME_TOPOLOGY_TESTS.md` status)

One-way generators from the cold start (owner decision D12, option O2). Branch `claude/cold-start-generators-wip`, base `7051391`. Nothing here is run by a Gradle task; the gates of this package are harness runs (the Gradle gates are due after the merge).

- `src/run.sh`: compiles every driver of `src/` against a tree's harness output and runs one: `REPO=<tree> LIB=<jars> tools/phase-ports-probes/d12/src/run.sh <class> [args]` after `tools/cloud-science-harness/harness.sh compile` on that tree. For the base, `git archive 7051391 src tools/cloud-science-harness | tar -x -C <dir>` and compile it with `REPO=<dir>`.
- `src/D12Probe.java` (package `...science.fluid.network`; args `grid|alternating|both spread pairs [closeBelowPa] [junctionSeedPa]`): builds an extreme-topology island, runs the cold rate seed's rate solve of the port graph and prints each generator's pressure, its junction, outflow and mode; the two optional arguments block the generators below a pressure and flash every junction at a uniform pressure (the section 1 matrix of the review). `logs/09-rate-seed-*.txt`.
- `src/D12Estimate.java` (args `geometry spread pairs sweeps`): the first estimator prototype, Gauss-Seidel on the junction pressures with start-state densities, then the rate solve and a step from its closures and seed; shows the sweep counts and the missed closures of review section 3.
- `src/D12StaticCompare.java`: the 32 `MixedGasJunctionStaticTest` cases with every result double printed; `logs/08-static-*.txt` (30 identical, 2 moved within 1.6e-11 kg/s).
- `src/D12Scan.java` (optional arg `quick` = first interval only): the `EXTREME_TOPOLOGY_TESTS.md` 4.4 scan, 60 cases x 2 slices; `logs/06-scan-base-7051391-first-interval.txt` (71 of 120 fail), `logs/06-scan-d12-full.txt` (120/120 over 40 x 5 s and 400 x 0.1 s).
- `src/sites-instrumentation.patch` (against `418ca7e`): prints where `closeColdReceivingGenerators` runs, closes or fails (`-Dd12.sites=true`, e.g. `HARNESS_JVM_OPTS="-Xmx2g -Dd12.sites=true"`) and the estimate's time (`-Dd12.time=true`); `logs/04-sites.txt`. `src/o1-first-step-only.patch` (against `418ca7e`): the O1-alone variant (the closure on any first solve, the cold rate seed disabled); `logs/05-o1-first-step-only.txt`. Apply with `git apply`, run, `git checkout` the files; never commit either.
- `logs/`: `01` harness all on `418ca7e` (science 213/213, runtime 237/237 with the 33 junction lines identical, adjacent 38/38, chain-100 0.000e+00); `02`/`03` the `ExtremeTopologyIslandTest` lines on the base and on D12; `07` the sha256 of the WP1/D9 `BitwiseProbe` outputs on base and D12 (byte-identical; run as the d9 README says); `10` the class with `FULL_CASES_OPEN_DEFECT = true` on D12 (the reproduction fails as designed).

## merge/ (merge commits `aaa6624` D12 and `334ca78` D13 into the D11 branch, review section "D11+D12+D13 merge")

Gate logs of the merged tree `334ca78`. Nothing here is run by a Gradle task.

- `logs/01-harness-all-merge.log`: cloud harness `all` (science 211/211, runtime 237/237 with the 33 junction lines identical, adjacent 38/38, chain-100 0.000e+00).
- `logs/02-gradle-fluid-suite-merge.log`, `02-test-names-merge.txt` (446 names: D11's 443 plus `VentGatePolishTest`'s 3), `02-junction-lines-merge.txt` (identical to `d10/logs/02-junction-lines-d10.txt` apart from ms/bytes/allocatedMB): the Gradle fluid suites.
- `logs/03` exact regression (0.000e+00), `logs/04` the 38 adjacent, `logs/05` GameTest and mcpCompat compile.
- `logs/06-fixture-summary-lines-merge.txt`: the `PHASE_PORT_PRIORITY_*`, `LEVEL_HEAD_RUN`, `VENT_GATE_RUN` and `EXTREME_TOPOLOGY` lines of `01`, compared in the review against `d11/logs/06` and `vent/logs/15`.
- `logs/07-d13-firings-per-test.txt`: D13 polish firings per @Test method, from harness `all` on a copy of `334ca78` with three prints added to `PassiveStepSolver` (never committed): after `SolverDiagnostics.count(equationGatePolishes)` (`D13_POLISH`), after `count(equationGatePolishesAccepted)` (`D13_ACCEPTED`, with the residual), and after `count(equationGateRejections)` (`D13_GATE_REJECTED`). Each print names the outermost `*Test` frame from `StackWalker`. To reproduce: `git archive 334ca78 src tools/cloud-science-harness | tar -x -C <dir>`, add the three prints, then `REPO=<dir> OUT=<out> LIB=<jars> tools/cloud-science-harness/harness.sh all`.

## wp3-wp4/ (code commit `fc9574d`, review section "WP3 + WP4")

Liquid-only pump, gas compressor and the inlet-phase refusal. Branch `claude/phase-ports-compressor`, base `85c492f`. Nothing here is run by a Gradle task.

- `src/CrudeFlashProbe.java` (default package): flashes the Tia Juana Light preset at 298.15 K and 350 K over 1 atm to 2 MPa and bisects its bubble point (`results/crude-flash.txt`: 11.15 % vapour by volume at 298.15 K / 1 atm, bubble point 117.8 kPa; 76.4 % at 350 K / 1 atm, 226.0 kPa), the flash the `PumpJunctionStartupTest` and crude GameTest re-baselines rest on.
- `src/Wp34Probe.java` (package `...science.fluid.network`): `f8` prints fixture 8's fill rates for two supply-line bores (`results/fixture8-fill-rates.txt`); `f4 <interval> <slices>` runs fixture 4's water line alone, with the refused pump line, with a passive both-way-blocked line and with a compressor closed both ways, the flows in hex (`results/fixture4-bitwise-variants-0.1s.txt`: refused = closed compressor and passive closed line = alone bit for bit; refused differs from alone in 40 of 60 slices at 0.1 s).
- `src/MigrationValueProbe.java` (package `...science.fluid.network`): the key numbers of the migrated tests that print none; run with `new` against the WP3+WP4 tree and with `old` against `85c492f` after `sed -E 's/NEW\?new FlowControl\.Compressor\([^)]*\):/ /g'` (HEAD has no `Compressor`). Outputs `results/migration-values-head-85c492f.txt` and `results/migration-values-wp34.txt`; `results/reports-old/`, `results/reports-new/` hold the P24, M3 and M4 JSON reports those qualification tests write, on HEAD and on the new tree.
- How to run: `tools/cloud-science-harness/harness.sh compile` on the tree (`REPO`, `OUT`, `LIB`), then `javac -d <out> -cp <OUT>/main:<OUT>/test:<gson, EJML> <probe>.java` and `java -cp <out>:<OUT>/main:<OUT>/test:<jars>:src/main/resources <class> <args>` from the tree's root. For HEAD, `git worktree add --detach <dir> 85c492f` (removed after use).
- `logs/`: `01` cloud harness all (223 + 238 + 39 + chain-100), `02` Gradle fluid suites (459 tests; `02-junction-lines-wp34.txt` identical to `d10/logs/02-junction-lines-d10.txt`; `02-test-names-wp34.txt`), `03` exact regression 0.000e+00, `04` the adjacent selection (39), `05` GameTest and mcpCompat compile, `06` the fixture summary lines of G1 (`INLET_PHASE_RUN`, `COMPRESSOR_RUN`, the migrated tests' prints), `07` Gradle `runtime.Fluid*` classes (17), `08` harness runs of the migrated classes, `09` harness runs of the new fixtures (first draft and final).
