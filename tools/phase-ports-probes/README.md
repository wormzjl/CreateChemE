# phase-ports-probes

Measurement drivers and gate logs of the batch `documentation/2026-09-26-phase-ports-and-compressor/`, one subfolder per work package. Nothing here is run by a Gradle task; the gates of record are the Gradle commands recorded in `PHASE_PORTS_REVIEW.md` of that batch. Written in the cloud container (OpenJDK 21.0.10, Linux); a Windows JDK 21.0.11 run differs in one ledger roundoff value (see the review, WP1 section 6).

## wp1/ (commit `35e354d`, review section 6-8 and 11)

- `src/BitwiseProbe.java`: gate G0b. Replays chain-100 and 14 all-BULK scenarios (mixed-gas junctions at 5 s and 0.1 s, rising/falling water lines, a pumped line, a wet-crude drain, a slurry filter, a valve) and dumps every double in hex; run on the base and on WP1, the outputs are byte-identical (`results/probe/out-base/`, `results/probe/out-new/`; `scenarios.txt` is the per-scenario dump, `chain-100.json` the regression encoding, byte-identical to `src/test/resources/fluid/regression/chain-100.json`).
- `src/JunctionProbe.java` and `src/instr/PassiveStepSolver.java`: the instrumented scratch copy of the reconstruction's equation gate used to locate the base defect of review section 8 (a junction seeded dry receiving unsaturated water vapour). The instrumented file is a copy of `PassiveStepSolver.java` at `6e1c5b6` with print statements; never build the mod with it.
- `src/HumidJunctionProbe.java`: reproduces that defect on `6e1c5b6` and on WP1 (identical gate residuals 1.3058e-8 at 0.5 % water, 2.6151e-8 at 1 %).
- `src/science-build.sh`: the first javac-only harness (science package and its Minecraft-free tests) used before Gradle could resolve dependencies in the container; superseded by `tools/cloud-science-harness/`.
- `logs/`: the WP1 gate logs, numbered as in the review: `01` cloud harness, `02` Gradle fluid suites on the clean base worktree (`6e1c5b6`; `02-junction-lines-base.txt` holds the 33 MIXED_GAS/LIQUID_JUNCTION lines, `02-test-names-base.txt` the 406 names), `03` the same on WP1 (413 tests, `03-junction-lines-wp1.txt` identical apart from ms/bytes), `04` exact regression 0.000e+00, `05` the 38 adjacent, `06` GameTest and mcpCompat compile, `07` javac science harness.

How to run a probe: compile the science sources (or use `tools/cloud-science-harness/harness.sh compile`) and `javac`/`java` the probe against that output with gson and EJML on the class path, as `science-build.sh` does; each probe prints where it writes its outputs.

## wp2/ (commit `d836cf2`, review WP2 sections 3-6 and 12)

- `src/PhasePortDutyCycleProbe.java`: risk R1. A LIQUID port that drains far faster than its vessel is fed (1 m3 N2 tank at 150 kPa, 2 % water, fed through a 4 mm line from a 200 kPa water generator, drained through a 25 mm LIQUID line to a void); counts open/closed transitions at 0.1 s and 5 s slices and at fixed 1 s steps (`logs/09-duty-cycle-probe.txt`: 45 / 9 / 5 transitions in 300 s). A JUnit class: copy it into `src/test/java/com/wormzjl/createcheme/science/fluid/network/`, run `tools/cloud-science-harness/harness.sh compile` then `select --select-class=com.wormzjl.createcheme.science.fluid.network.PhasePortDutyCycleProbe`, and delete the copy.
- `src/SqueezeGateProbe.java`: the squeeze fixture with BULK or phase ends (a method to paste into `PhasePortClosureTest`), classifying the VAPOR-vent equation-gate rejections as WP1 behaviour (`logs/12-*`: 2 on WP2, 4 on the WP1 tree `3dbd9b8`, 0 with a BULK vent). For the WP1 tree, `git archive 3dbd9b8 src | tar -x -C <dir>`, replace `PassiveStepSolver.portPhaseShare`/`PHASE_PORT_*` in the copied test with local equivalents, and run the harness with `REPO=<dir>`.
- `src/counterfactual-reopen-no-mask.patch` (against `d836cf2`): the reopen test reading an all-zero mask; `logs/10-*` show 20 (5 s) and 322 (0.1 s) spurious reopens on `thePortAloneRefusesOutflow...`, against 1 and 1. `src/gate-instrumentation.patch`: prints the gate's worst row (`logs/11-*`: the tank's local row 4, 4.45e-8 and 2.06e-7). Apply with `git apply`, measure, `git checkout` the file; never commit either.
- `logs/`: `wp2-01` cloud harness all, `wp2-02` Gradle fluid suites (428 tests; `-junction-lines.txt` identical to `wp1/logs/02-junction-lines-base.txt` with ms/bytes masked; `-test-names.txt`), `wp2-03` exact regression 0.000e+00, `wp2-04` the 38 adjacent, `wp2-05` GameTest and mcpCompat compile, `wp2-06`/`wp2-07` the G1/G2 re-run on the final tree, `08` the harness run of `PhasePortClosureTest` with every slice line.

## d10/ (commit `07e7426`, review section "D10")

- `src/trace-sites.patch` (against `07e7426`): prints the calling test each time a junction gets the water trace; applied to the harness science, runtime and adjacent runs it fires only in `JunctionWaterTraceTest` (`logs/07-trace-sites.txt`). Apply with `git apply`, run, `git checkout` the file; never commit it.
- The humid-junction reproduction is `wp1/src/HumidJunctionProbe.java` (compile it against the harness's main classes plus `src/main/resources`, gson and EJML): `logs/06-humid-probe-d10.txt` all six cases integrate, `logs/06-humid-probe-wp2.txt` the 0.5 % and 1 % cases fail as before.
- `logs/`: `01` cloud harness all, `02` Gradle fluid suites (430 tests; **`02-junction-lines-d10.txt` is the junction-line base for WP3**, identical to `wp1/logs/02-junction-lines-base.txt`), `03` exact regression 0.000e+00, `04` the 38 adjacent, `05` GameTest and mcpCompat compile, `08` the WP1 bitwise probe's scenario dumps on the WP2 and D10 trees (byte-identical).

## d9/ (commit `fdf3574`, review section "D9")

- `src/BitwiseProbe.java`: the WP1 probe (gate G0b) extended with `gas-ports.txt`, eight gas-only islands whose vessels carry LIQUID and VAPOR ports (a N2 generator into a LIQUID port, a VAPOR-to-LIQUID tank pair and a methane/nitrogen junction fed through phase ports, each at 5 s and 0.1 s; a dead-headed rising LIQUID port; a dry LIQUID drain), which the head must leave bitwise, and `liquid-ports.txt`, a rising line and a drain whose LIQUID port sees water (expected to move; not a gate). Run on `0cf4ec9` (WP2 + D10, before the edit) and on the D9 tree: `results/probe/out-wp2/`, `results/probe/out-d9/`, `results/probe/scenarios.txt` (identical on both), `results/probe/sha256.txt` (every file of both runs plus the regression reference; `chain-100.json`, `scenarios.txt`, `gas-ports.txt` identical). Compile and run as the wp1 probe: `javac -d <out> -cp <harness OUT>/main:<gson, EJML> BitwiseProbe.java`, `java -cp <out>:<harness OUT>/main:src/main/resources:src/generated/resources:<jars> BitwiseProbe <output dir>`.
- `src/DrainProbe.java`, `src/ManometerProbe.java` (package `com.wormzjl.createcheme.science.fluid.network`, compile against the harness's main classes as above; arguments in each file's javadoc): the classification of the equation-gate rejections met by the D9 fixtures. `logs/08-vapor-vent-gate-classification.txt`: a VAPOR vent on a tank filled through its LIQUID port exhausts the substep limit on the WP2 + D10 tree as on D9 (WP2 option 3 class); `logs/08-drain-gate-classification.txt`: the head drain on D9 against the WP2 tree with the head emulated by a lower void.
- `src/mutations/no-head-in-{closeDeadHeads,reopenable,edgeRows}.patch` (against `fdf3574`): each removes the head from one site; `logs/09-mutation-*.txt` show which `LevelHeadTest` cases catch it (3, 1 and 7 of 8 fail). Apply to a copy of the tree (`git apply`), run the harness `select --select-class=...LevelHeadTest` with `REPO=<copy>`; never commit them.
- `logs/`: `01` cloud harness all (213 + 237 + 38 + chain-100), `02` Gradle fluid suites (448 tests; `02-junction-lines-d9.txt` identical to `d10/logs/02-junction-lines-d10.txt`; `02-test-names-d9.txt`), `03` exact regression 0.000e+00, `04` the 38 adjacent, `05` GameTest and mcpCompat compile, `06` the `LEVEL_HEAD_*` and phase-port summary lines of G1, `07` the phase-port summary lines on the WP2 + D10 tree (for the moved numbers), `08` gate classification, `09` mutations, `10`/`11` the G1/G2 re-run on the final tree.

## extreme/ (commit `55508e4`, `documentation/2026-09-26-phase-ports-and-compressor/EXTREME_TOPOLOGY_TESTS.md`)

- `src/ExtremeTopologyScanProbe.java`: the temporary scan test method (kept out of `src/test`) that located the pass/fail boundary of the owner's two extreme block layouts (`ExtremeTopologyIslandTest`): column count, pressure spread and velocity cap, plus the water variant. Paste it into the test class to rerun; it needs the same builders.
